# Salesforce
.sf/
.sfdx/
*.lock

# Jira / GWB source data — never commit
PR*.csv
PR*.json
*.csv

# Exclude loose JSON files at root (Jira exports etc.) but keep project config
/*.json
!sfdx-project.json

# macOS
.DS_Store

# Environment / secrets
.env
*.env.*
!.env.example

# Node
node_modules/

# MuleSoft application config (contains credentials — never commit)
plantiq-integration/src/main/resources/application.properties
plantiq-integration/src/main/resources/application-*.properties

# Anypoint Studio IDE files
plantiq-integration/.project
plantiq-integration/.classpath
plantiq-integration/.settings/
plantiq-integration/target/
plantiq-integration/**/.mule/
plantiq-integration/**/ehcache-disk-store.*
